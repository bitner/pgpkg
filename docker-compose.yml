services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: pgpkg
      POSTGRES_PASSWORD: pgpkg
      POSTGRES_DB: pgpkg
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pgpkg"]
      interval: 2s
      timeout: 2s
      retries: 30
